# Scripts

Placeholders for:

- export curated windows from the training lab into HF-ready layout
- checksum / split validation
- `huggingface-cli` upload helpers

Do not commit window binaries; upload those to Hugging Face.
